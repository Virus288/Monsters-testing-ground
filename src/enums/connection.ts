export enum EConnectionChannels {
  Connection = 'connection',
  Update = 'update',
}

export enum EResponseCallback {
  Data = 'data',
  Error = 'error',
  Client = 'client',
  Log = 'log',
  Version = 'version',
}
