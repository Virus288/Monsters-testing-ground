export enum EConnectionChannels {
  Connection = 'connection',
  Update = 'update',
}

export enum EResponseCallback {
  Data = 'data',
  Error = 'error',
  Debug = 'debug',
  Client = 'client',
  Log = 'log',
  Version = 'version',
}
