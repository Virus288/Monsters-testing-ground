export enum EConnectionChannels {
  Connection = 'connection',
  Main = 'main',
}

export enum EResponseCallback {
  Data = 'data',
  Error = 'error',
  Client = 'client',
  Log = 'log',
  Version = 'version',
}
