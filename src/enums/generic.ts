// eslint-disable-next-line import/prefer-default-export
export enum EGenericChannel {
  Init = 'init',
  Register = 'register',
  Login = 'login',
  Response = 'response',
  Tokens = 'tokens',
  ChatMessage = 'chatMessage',
}
