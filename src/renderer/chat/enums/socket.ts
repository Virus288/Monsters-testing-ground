export enum ESocketActionType {
  Logs = 'logs',
  Messages = 'messages',
  Status = 'status',
}

export enum ESocketTarget {
  SendManual = 'sendManual',
  SendMessage = 'sendMessage',
  Logs = 'logs',
  Messages = 'messages',
}
